import 'package:flutter/material.dart';

class WordBlockPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'This is the Word Block Page',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}