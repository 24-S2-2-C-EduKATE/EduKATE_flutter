import 'package:flutter/material.dart';

class TextBlockPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'This is the Text Block Page',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}